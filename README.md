# 🎯 เกมทายตัวเลข 3 หลัก

เกมทายตัวเลข 3 หลักที่ช่วยให้คุณหาคำตอบที่ถูกต้องได้อย่างชาญฉลาด! ระบบจะกรองคำตอบที่เป็นไปได้ตามข้อมูลที่คุณใส่ และแนะนำตัวเลขสำหรับการทายครั้งถัดไป

## ✨ ฟีเจอร์

- **ระบบกรองอัตโนมัติ**: คำนวณคำตอบที่เป็นไปได้ตามข้อมูลที่ใส่
- **แนะนำตัวเลข**: แสดงตัวเลขที่แนะนำสำหรับการทายครั้งถัดไป
- **แก้ไข/ลบประวัติ**: สามารถแก้ไขหรือลบข้อมูลการทายที่ใส่ผิดได้
- **สถิติแบบเรียลไทม์**: แสดงจำนวนครั้งที่ทายและคำตอบที่เป็นไปได้
- **กฎพิเศษ**: ตัวเลขแต่ละหลักต้องไม่ซ้ำกันและไม่มีเลข 0
- **UI สวยงาม**: ออกแบบด้วย CSS สีฟ้า สวยงามและใช้งานง่าย

## 🚀 วิธีการรัน

### วิธีที่ 1: เปิดไฟล์โดยตรง
1. ดาวน์โหลดไฟล์ทั้งหมด
2. เปิด `index.html` ในเบราว์เซอร์


## 🎮 วิธีการเล่น

1. **ใส่ข้อมูลการทาย**: ใส่ตัวเลข 3 หลักที่คุณทาย + จำนวนที่ถูกตัวเลข + จำนวนที่ถูกตำแหน่ง
2. **ดูคำแนะนำ**: ระบบจะแสดงตัวเลขที่แนะนำสำหรับการทายครั้งถัดไป
3. **แก้ไขข้อมูล**: หากใส่ผิด สามารถคลิก ✏️ เพื่อแก้ไขหรือ 🗑️ เพื่อลบ
4. **เริ่มใหม่**: คลิก "🔄 เริ่มใหม่" เพื่อเริ่มเกมใหม่

### ตัวอย่าง

```
ใส่: 123
ถูกตัวเลข: 1 ตัว
ถูกตำแหน่ง: 1 ตำแหน่ง

ระบบจะกรองคำตอบที่เป็นไปได้ และแนะนำตัวเลขถัดไป เช่น 145, 167, 189 เป็นต้น
```

## 📁 โครงสร้างไฟล์

```
/
├── index.html      # ไฟล์ HTML หลัก
├── style.css       # ไฟล์ CSS สำหรับสไตล์
├── script.js       # ไฟล์ JavaScript สำหรับฟังก์ชัน
└── README.md       # ไฟล์นี้
```

## 🛠️ เทคโนโลยีที่ใช้

- **HTML5**: โครงสร้างหน้าเว็บ
- **CSS3**: การออกแบบและสไตล์
- **Vanilla JavaScript**: ตรรกะและการคำนวณ

## 📊 ข้อมูลทางเทคนิค

- **จำนวนคำตอบเริ่มต้น**: 504 ตัว (เลข 1-9 ไม่ซ้ำกัน ไม่มีเลข 0)
- **อัลกอริธึม**: กรองแบบ incremental และ full recalculation เมื่อแก้ไข
- **การตรวจสอบ**: ตัวเลขไม่ซ้ำกันและไม่มีเลข 0
